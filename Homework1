#the sum of quadras of 2 max numbers
def sum_of_quadr(a,b,c):
    if a > c and b > c: 
      return a**2 + b**2
    
    elif a > b and c > b:
      return a**2 + c**2
    
    return b**2 + c**2
    
    
print(sum_of_quadr(6,9,3))
      
    
